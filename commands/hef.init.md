---
model: sonnet
description: "Bootstrap .specify/ directory for SDD workflow in current project"
---

# Project Initialization

Bootstrap a `.specify/` directory in the current project to enable spec-driven development.

## Pre-Flight Checks

### Git root detection
> The commands in this section must be run with the Bash tool; they cannot be
> pre-executed in a `!` block. A `!` block is permission-checked before the
> CLAUDE_PLUGIN_ROOT variable is substituted, so it is rejected as "Contains
> expansion". Do not write that variable with a $ and braces here: it would be
> substituted into this note and the warning would read as nonsense.

Run with the Bash tool: `${CLAUDE_PLUGIN_ROOT}/hooks/speckit-helper.sh check-git-root`

### Existing .specify/ detection
Run with the Bash tool: `${CLAUDE_PLUGIN_ROOT}/hooks/speckit-helper.sh check-specify-dir`

## Instructions

1. **Check pre-flight results above**
   - If `NOT_A_GIT_REPO`: warn the user but allow proceeding (specs still work without git)
   - If `.specify/` `EXISTS`: ask the user whether to reinitialize (overwrite templates only) or abort

2. **Create directory structure**
   ```
   .specify/
   ├── memory/
   │   └── constitution.md    (skeleton)
   ├── templates/
   │   ├── spec.md
   │   ├── plan.md
   │   ├── tasks.md
   │   └── checklist.md
   └── specs/                  (empty, branch specs go here)
   ```

3. **Create skeleton `memory/constitution.md`**

   Write this content:
   ```markdown
   # Project Constitution
   <!-- Generated by /hef.init — customize with /hef.constitution -->
   <!-- Version: 0.1.0 | Date: YYYY-MM-DD -->

   ## Principles

   1. **[Principle 1]** — _Not yet defined. Run `/hef.constitution` to populate._
   2. **[Principle 2]** — _Not yet defined._
   3. **[Principle 3]** — _Not yet defined._
   4. **[Principle 4]** — _Not yet defined._
   5. **[Principle 5]** — _Not yet defined._

   ## Tech Stack
   _Run `/hef.constitution` to auto-detect and record._

   ## Architecture Constraints
   _Run `/hef.constitution` to define._

   ## References
   - Project rules: `.claude/rules/`
   ```

4. **Create `templates/spec.md`**

   Write this content:
   ```markdown
   # Spec: <BRANCH_NAME>
   <!-- Auto-generated by /hef.spec -->

   ## Overview
   _Brief description of the feature._

   ## User Scenarios

   ### US1: <Scenario Name> [P1]
   - **Given** <precondition>
   - **When** <action>
   - **Then** <expected outcome>

   ## Functional Requirements

   | ID | Requirement | Priority | Scenario |
   |----|-------------|----------|----------|
   | FR-001 | _description_ | P1 | US1 |

   ## Success Criteria

   | ID | Criterion | Validation Method |
   |----|-----------|-------------------|
   | SC-001 | _description_ | _how to verify_ |

   ## Clarifications
   _None yet._
   ```

5. **Create `templates/plan.md`**

   Write this content:
   ```markdown
   # Plan: <BRANCH_NAME>
   <!-- Auto-generated by /hef.plan -->

   ## Research Notes
   _Resolved clarifications and technical findings._

   ## Data Model
   _Key entities and relationships. See `data-model.md` for full schema if needed._

   ## API Contracts
   _Endpoint signatures, request/response shapes. See `contracts/` for details if needed._

   ## Implementation Approach
   _High-level strategy, key decisions, trade-offs._

   ## Constitution Compliance
   - [ ] Principle 1: _how this plan aligns_
   - [ ] Principle 2: _how this plan aligns_

   ## Quick Start
   _Steps to begin implementation after plan approval._
   ```

6. **Create `templates/tasks.md`**

   Write this content:
   ```markdown
   # Tasks: <BRANCH_NAME>
   <!-- Auto-generated by /hef.tasks -->

   ## Phase 1: Setup
   - [ ] T001 [US1] _Setup task description_ `path/to/file`

   ## Phase 2: Foundational
   - [ ] T002 [US1] _Foundation task_ `path/to/file`

   ## Phase 3: User Stories (by priority)
   ### P1
   - [ ] T003 [P] [US1] _Implementation task_ `path/to/file`

   ### P2
   - [ ] T004 [US2] _Implementation task_ `path/to/file`

   ## Phase 4: Polish
   - [ ] T005 [US1] _Polish task_ `path/to/file`

   ---
   Legend: `[P]` = parallelizable | `[US#]` = user scenario ref
   ```

7. **Create `templates/checklist.md`**

   Write this content:
   ```markdown
   # Checklist: <TYPE>
   <!-- Auto-generated by /hef.checklist -->

   ## Requirements Quality

   | ID | Check | Dimension | Status |
   |----|-------|-----------|--------|
   | CHK001 | _check description_ | [completeness] | [ ] |
   | CHK002 | _check description_ | [consistency] | [ ] |
   | CHK003 | _check description_ | [testability] | [ ] |
   ```

8. **Post-init guidance**
   - Tell the user: "`.specify/` has been created. Commit it to version control so your team shares the same spec context."
   - Suggest: `git add .specify/ && git commit -m "chore: initialize .specify/ scaffolding"`
   - Mention next steps: run `/hef.constitution` to define project principles
